# Aulas

Apresentações (slides revealjs) usadas em sala de aula. Cada `.qmd` define seu próprio bloco `format: revealjs`, com tema em `../TemaRTx.scss`.

Imagens e figuras (TikZ, logos) ficam em [imgs/](imgs/).
